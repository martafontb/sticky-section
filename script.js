ScrollTrigger.create({
		trigger:".box",
		start:"top top",
		end:"+=140%",
		markers:true,
		pin:true
})

ScrollTrigger.create({
	trigger:".img",
	start:"top top",
	end:"+=140%",
	markers:true,
	pin:true	
})